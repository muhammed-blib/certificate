node 'slave1' {
    file { '/temp': 
        ensure => 'directory',
    }
    file { '/temp/slave1': 
        content => 'this is slave1',
    }
}

node 'slave2' {
    file { '/temp': 
        ensure => 'directory',
    }
    file { '/temp/slave2': 
        content => 'this is slave2',
    }
}

node 'slave3' {
    file { '/temp': 
        ensure => 'directory',
    }
    file { '/temp/slave3': 
        content => 'this is slave3',
    }
}
